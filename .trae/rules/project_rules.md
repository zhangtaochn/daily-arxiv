# 调试说明
这是一个github action项目，一些重要的变量，类似于 API_KEY 是写在github 的环境变量里。
为方便本地调试，我将这些变量写在了./tmp/run.sh 文件里，在本地运行时，执行了环境变量，从而可以顺利调试。

为了保护环境变量的安全性，我将 ./tmp/run.sh 文件添加到了 .gitignore 中，避免将环境变量提交到代码仓库。

因此，如果你需要测试代码是否可以运行，你需要执行 bash ./tmp/run.sh 

